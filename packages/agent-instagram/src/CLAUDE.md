# agent-instagram/src

Root of the agent-instagram source tree. Contains the package entry point (`index.ts`) and top-level configuration (`config.ts`). Coordinates all sub-modules: adapters, commands, state, and utils.

> E2E verification requirements live in the flow graph — see flow skills via `npx agent-core search "flow/" --type skill`
