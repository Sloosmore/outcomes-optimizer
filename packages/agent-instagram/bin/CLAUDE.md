# agent-instagram/bin

Entry point for the agent-instagram CLI. Contains a single script (`agent-instagram.js`) that bootstraps the compiled TypeScript by importing `../dist/index.js`.

> E2E verification requirements live in the flow graph — see flow skills via `npx agent-core search "flow/" --type skill`
