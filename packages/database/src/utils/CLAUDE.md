# Database Utilities

Internal helpers for the database package. Not exported directly — consumed by services and commands within the package.

## Files

- `process-dependencies.ts` — Resolve process dependency chains
- `resource-helpers.ts` — Common resource query/mutation helpers

> E2E verification requirements live in the flow graph — see flow skills via `npx agent-core search "flow/" --type skill`
