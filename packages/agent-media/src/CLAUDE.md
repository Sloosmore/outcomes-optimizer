# agent-media/src

Root of the agent-media source tree. Contains the package entry point (`index.ts`) and top-level configuration (`config.ts`). Wires together adapters, commands, state, and utils.

> E2E verification requirements live in the flow graph — see flow skills via `npx agent-core search "flow/" --type skill`
