# agent-media/bin

Entry point for the agent-media CLI. Contains a single script (`agent-media.js`) that bootstraps the compiled TypeScript by importing `../dist/index.js`.

> E2E verification requirements live in the flow graph — see flow skills via `npx agent-core search "flow/" --type skill`
