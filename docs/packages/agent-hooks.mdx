---
title: "agent-hooks"
description: "Git hook management for outcomes-optimizer."
---

## Install

```bash
npx agent-hooks --help
```

## Purpose

Installs and manages git hooks (pre-commit, commit-msg) that enforce skill linting, changelog validation, and stray file detection before each commit.

## Files

```
packages/agent-hooks/
└── src/
    └── index.ts
```
