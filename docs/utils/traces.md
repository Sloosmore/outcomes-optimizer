# Trace Retrieval

> **Status:** Removed

The trace retrieval CLI (`utils/traces/`) was deleted as dead code. See git history for the original implementation.

Trace metadata and storage remain in the database (`traces` table and Supabase Storage `traces` bucket).
