# runtime/skills

Skill definitions deployed to and executed by the runtime service. Each subdirectory is a distinct skill with its own `SKILL.md` describing prerequisites, usage patterns, and configuration requirements.

> E2E verification requirements live in the flow graph — see flow skills via `npx agent-core search "flow/" --type skill`
