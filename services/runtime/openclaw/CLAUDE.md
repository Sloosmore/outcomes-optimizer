# runtime/openclaw

Configuration for the openclaw agent runtime — the process that runs Claude agents on this server. The `config/` directory holds the primary configuration file (`openclaw.json`) that controls model selection, concurrency limits, gateway settings, and channel integrations.

> E2E verification requirements live in the flow graph — see flow skills via `npx agent-core search "flow/" --type skill`
