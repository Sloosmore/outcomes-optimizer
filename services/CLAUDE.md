# Services

Each subdirectory is a deployed infrastructure service. Before writing any code for a new service, the first question to answer is: **how will you verify it works end-to-end in production?**

> E2E verification requirements live in the flow graph — see flow skills via `npx agent-core search "flow/" --type skill`
