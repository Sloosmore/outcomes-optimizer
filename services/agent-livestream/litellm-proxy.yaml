model_list:
  - model_name: claude-haiku-4-5-20251001
    litellm_params:
      model: openai/gpt-5.4-mini
      api_key: os.environ/OPENAI_API_KEY

  # Fallback — catch any other claude model name the SDK might send.
  # Model ID mirrors CLAUDE_MODEL in utils/types.ts — update both together.
  - model_name: claude-sonnet-4-6
    litellm_params:
      model: openai/gpt-5.4-mini
      api_key: os.environ/OPENAI_API_KEY

litellm_settings:
  drop_params: true   # ignore unsupported Anthropic-only params
