# dispatch/provisioners/__tests__

Unit tests for dispatch provisioners. Tests mock git, pnpm, and DB calls — no real infrastructure.

> E2E verification requirements live in the flow graph — see flow skills via `npx agent-core search "flow/" --type skill`
